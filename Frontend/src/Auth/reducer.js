import { GET_COORDINATES_CITY_REQUEST } from "./actionTypes";

export const initialState = {
  content: "login",
};

export default (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
